раньше этот проект был канбан доской. но я хочу его переделать в контент-планнер.

столбцы в @src/components/BoardTable.vue (а также @src/db/db.ts и в других местах) нужно переделать на дни недели: ПН, ВТ, СР, ЧТ, ПТ, СБ, ВС. неделя начинается с понедельника.

после создания новой доски автоматически добавляется новая история. эта первая история должна быть шириной во все столбцы (использовать colspan или аналогичное свойство). ее название должно быть "Categories".

у задач больше не будет поля assignee. нужно убрать его из @src/components/TaskCard.vue @src/db/db.ts и других местах. цвет задачи теперь зависит от ее текста, а не от текста assignee.

нужно обновить @src/db/db.ts
т.к. это новый проект, то DB_VERSION должен быть равен 1. из миграций нужно убрать вторую версию. а первую миграцию обновить (добавить туда slug из второй миграции). при работе с idb используй документацию @docs/packages/idb.md

